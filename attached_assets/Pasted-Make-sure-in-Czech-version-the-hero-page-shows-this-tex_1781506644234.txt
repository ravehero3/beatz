Make sure in Czech version the hero page shows this text “Česká tržiště beatů pro nezávislé producenty. Prodávej beaty přímo kupujícím — nech si vše, co vyděláš.” As “České tržiště beatů pro nezávislé producenty. Prodávejte beaty přímo kupujícím — nechte si vše, co vyděláte.” Instead

Also it seems like the Tvoje Beaty. Tvůj obchod to “Vaše Beaty. Váš obchod.” And make sure the lower parts of letters from this text aren’t blocked by anything (right now the font looks like its cut in the bottom parts of the letters “j” and “y”   also I noticed the hero page in Czech language says a lot of “tvůj” “Dostávej” etc.. in Czech Republic its considered rude to talk like this and customers like more the “vykání” which means we have to rewrite the Czech copy in the Jak to funguje section and the Otevři svůj beat store ještě dnes. All of texts inside these sections need to be rewritten so they talk like “Nahrajte beaty” instead of “Nahraj beaty” 

Also lets change the card background that appears in the pricing cards when we hover over the Pro or PRO+ cards. It looks horrible. Let’s change the hover effect of the cards to be something more professional. I really like liquid glass. Do you think it’s possible to make a hover card. 
I have a pricing card component in my web app and I want to add a mouse-tracking 3D tilt effect to it. When the user hovers over the card, it should dynamically tilt in 3D perspective toward wherever their cursor is — fully reactive to mouse position in real time.
Add this behavior to my existing pricing card:
1. On mousemove inside the card, calculate the cursor's X and Y position relative to the card's bounding box, normalize it to a -0.5 to 0.5 range, then apply a perspective(700px) rotateX(Xdeg) rotateY(Ydeg) transform. Use about 16 degrees of max rotation. Invert the Y axis for rotateX so the card tilts naturally toward the cursor.
2. Also update box-shadow dynamically on each mousemove to shift in the opposite direction of the tilt, so the light source feels consistent.
3. On mouseleave, reset the transform and shadow back to their defaults with a smooth transition: transform 0.4s ease so it eases back gently.
4. During mousemove, use a very short transition (0.1s) so the tilt tracks the cursor without lag.
5. Make sure will-change: transform and transform-style: preserve-3d are set on the card for GPU acceleration.
Apply this to whatever element has my pricing card class/ID. Keep all other card styles untouched.

Also we need to redesign the 3 different beat players. We don’t really need 3 different sizes. We need one player that fits a playlist of beats that shows the BPM, KEY and TAGS for each beat as well as the add to cart button (icon) 

Notice how the studio in desktop has a lot of free space in the right side of the page. we should use this space to show live preview of the users storefront when its applicable. For example when user is adding a beat, we should see a card with the beats artwork, name, length sound wave of the timeline (in past we used ffmpeg to calculate that but each time the user had to press button, is there a way to do this automatically so it loads the sound wave when user uploads a beat? the one we used before used 88 thin bars to show the whole timeline of a beat that is usually 3 minutes long) the card should also show the info with correct padding and the card design should be using the same design elements as we have on the hero page below h1. 

Basically the white space on the right side of the page in studio should be used as preview zone where the user sees live changes when editing their shop design.

The default template that also shows up in live preview should be perfect. It should look like a professional web designer who uses the latest approaches to web design that are used in 2026. The users beats should be shown on the main page of their shop. their shop should show their logo in the header centre and clicking on their logo should open a new Glassmorphism modal (same design techniques as the previous Glassmorphism cards from hero page of our landing page) the live preview should show the playlist with beats. If there are no beats, then the playlist should have empty slots.  we should also implement a new feature in our app. Sticky player that has the same design as cards before but different size. It should have correct padding so it’s a little bit above the lower edge of the page. It should show the beats  timeline so user can click anywhere on the timeline to preview that part of the beat that is being played. It should have play button that changes into pause button when pressed. It should show all the information about the beat within the player. The player should have padding correctly set within every element it has and its design should be perfect

When viewing the studio, the header from pages before should persist in its place and the “studio” should appear below the logo in the header when viewing the studio page. Also change the “STUDIO” text so it has correct font and padding to the logo so its 2px below the logo and little bit to the right

Also the studio should show all text in Czech when we are switched to CZ. Make sure we have CZ/EN toggle in the footer of all pages (bottom right)